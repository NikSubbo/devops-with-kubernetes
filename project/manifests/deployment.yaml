apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      volumes:
        - name: shared-pvc
          persistentVolumeClaim:
            claimName: project-pv-claim
      containers:
        - name: todo-app
          image: niksubbo/todo-app:1508a33eb4eb0b8b149a7cf5c7b92377a9a3507a
          env:
            - name: PORT
              value: "5000"
          volumeMounts:
            - name: shared-pvc
              mountPath: /app/files
